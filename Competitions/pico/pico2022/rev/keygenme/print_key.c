
int main() {
  pico_CTF()_length = strlen((char *)&pico_CTF{);
  MD5((uchar *)&pico_CTF{,pico_CTF()_length,pico_CTF{_raw_hash);
  pico_CTF()_length = strlen((char *)&});
  MD5((uchar *)&},pico_CTF()_length,}_hash);
  N = 0;
  for (i = 0; i < 16; i = i + 1) {
    sprintf(readable_bytes_pico_CTF{_hash + N,"%02x",(uint)pico_CTF{_raw_hash[i]);
    N = N + 2;
  }
  N = 0;
  for (c = 0; c < 16; c = c + 1) {
    sprintf(readable_bytes_}_hash + N,"%02x",(uint)}_hash[c]);
    N = N + 2;
  }
  for (n = 0; n < 27; n = n + 1) {
    all_bytes[n] = *(char *)((long)&pico_CTF{ + (long)n);
  }
}